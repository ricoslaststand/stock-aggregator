import { combineVariances, variance  } from "simple-statistics"

export function calculateStockBeta(stockPriceChanges: number[], indexPriceChanges: number[]): number | undefined {
    
}

export function calculateStockVolatility(stockPrices: number[]): number | undefined {
    return undefined
}